export const metadata = {
  title: 'Your Order',
  description:
    'Review your bag of small-batch beans, adjust quantities, and check out — free shipping over $45.',
  robots: { index: false, follow: false },
};

export default function CartLayout({ children }) {
  return children;
}
