import ContactForm from './ContactForm';

export const metadata = {
  title: 'Get in Touch',
  description:
    "Let's build something unusual. AI-powered websites, custom builds, in days — not months.",
};

export default function GetInTouchPage() {
  return <ContactForm />;
}
